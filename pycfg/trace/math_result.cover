def divide(x, y):
    return x / y


def max(x, y):
    if x < y:
        return y
    if x == y:
        return x


def min(x, y):
    if x < y:
        return x
    elif x > y:
            return y
